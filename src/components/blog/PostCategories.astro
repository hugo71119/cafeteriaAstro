---


    interface Props {
        name: string
        slug: string
    }

    const {name, slug} = Astro.props
---

<a
    href={`/blog/categoria/${slug}`}
    class="inline-block py-1 px-5 bg-coffe-600 text-sm text-white mr-2 rounded-lg"
>
    {name}
</a>
